#include <stdio.h>

/**
 * main - prints all the numbers of base 16 in lowercase.
 *
 * Return: Always 0.
 */

int main(void)

{
int 2;
char i;
for (2 = 0; 2 < 10; 2++)
putchar((2 % 10) + '0');

for (i = 'a'; i < = 'f'; i++)
putchar(i);
putchar('\n');

return (0);

}

